import instance from "@/utils/axios.utils";

const chat = {
  chats: (body: any) => {
    let promise = new Promise((resolve, reject) => {
      let url = `properties/chat-flow/`;
      instance()
        .post(url, body)
        .then((res) => {
          resolve(res.data);
        })
        .catch((error) => {
          if (error.response) {
            reject(error.response?.data);
          } else {
            reject(error);
          }
        });
    });
    return promise;
  },

  callback: (body: any) => {
    let promise = new Promise((resolve, reject) => {
      let url = `callbacks/`;
      instance()
        .post(url, body)
        .then((res) => {
          resolve(res.data);
        })
        .catch((error) => {
          if (error.response) {
            reject(error.response?.data);
          } else {
            reject(error);
          }
        });
    });
    return promise;
  },

  booking_inquiry: (body: any) => {
    let promise = new Promise((resolve, reject) => {
      let url = `apartment-callbacks/`;
      instance()
        .post(url, body)
        .then((res) => {
          resolve(res.data);
        })
        .catch((error) => {
          if (error.response) {
            reject(error.response?.data);
          } else {
            reject(error);
          }
        });
    });
    return promise;
  },

  global: (body: any) => {
    let promise = new Promise((resolve, reject) => {
      let url = `ai-flow/`;
      instance()
        .post(url, body)
        .then((res) => {
          resolve(res.data);
        })
        .catch((error) => {
          if (error.response) {
            reject(error.response?.data);
          } else {
            reject(error);
          }
        });
    });
    return promise;
  },

  global_property: (body: any) => {
    let promise = new Promise((resolve, reject) => {
      let url = `properties/ai-flows/`;
      instance()
        .post(url, body)
        .then((res) => {
          resolve(res.data);
        })
        .catch((error) => {
          if (error.response) {
            reject(error.response?.data);
          } else {
            reject(error);
          }
        });
    });
    return promise;
  },
  
  

};

export default chat;
